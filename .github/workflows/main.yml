name: CI-Python

on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: pip came to town
        run: pip install -r requirements.txt
    
      - name: run forest run
        run: python3 app.py &
        
      - name: test
        run: sleep 5 && curl localhost:8008
        
     

  
